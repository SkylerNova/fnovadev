
{% include 'nosto-element' with 'searchpage-nosto-1' %}



<div id="searchresults" class="container">  

  <h1 class="title">Search Results</h1>

  {% if search.performed %}

    {% paginate search.results by 36 %}  

    {% if search.results == empty %}  
      <p class="note">Your search for "{{search.terms | escape}}" did not yield any results.</p>
      <div class="search-form">
        <form action="/pages/search-results" method="get" class="search-form">
          <div class="clearfix">
            <input type="hidden" name="type" value="product" />
            <input type="text" class="input-text animation" name="q" value="" accesskey="4" autocomplete="off" placeholder="Search">
            <input type="submit" class="input-icon right fa" value="&#xf002;" >
          </div>
        </form>
      </div>
    {% else %}
      <div id="collection" class="clearfix">
        <div class="grid-uniform">
          {% for item in search.results %}
            {% assign product = item %}
            {% include 'product_item', fromSearch: true %}
          {% endfor %}
        </div>
      </div>
    {% endif %}
    {% include 'pagination' %}
    {% endpaginate %}

  {% endif %}  

</div>

{% include 'nosto-element' with 'searchpage-nosto-2' %}
