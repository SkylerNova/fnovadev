<section class="quick-links container">
  <div class="grid">
    {% for item in linklists.homepage-quick-links.links %}
      <div class="grid__item item small--one-half text-center">
        <a href="{{ item.url }}"><div>{{ item.title }}</div></a>
      </div>
    {% endfor %}
  </div>
</section>